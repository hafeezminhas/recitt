import { Environment } from './env.interface';

export const environment: Environment = {
  production: true,
  apiPrefix: '/api',
  apiMockDelay: 0,
  version: '1.0.0',
  appName: 'Recitt',
  credentials: null,
  defaultLanguage: 'en',
  authKey: 'xapi-key',
};
