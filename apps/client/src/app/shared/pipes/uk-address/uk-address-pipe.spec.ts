import { UkAddressPipe } from './uk-address-pipe';

describe('UkAddressPipe', () => {
  it('create an instance', () => {
    const pipe = new UkAddressPipe();
    expect(pipe).toBeTruthy();
  });
});
